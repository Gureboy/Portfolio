/* dnd-easter-egg.css
   Estilos para el juego roguelike D&D Easter Egg - Integrado con Portfolio */

:root {
  --bg-dark: #0a0e1a;
  --bg-secondary: #1a2332;
  --bg-panel: #2d3748;
  --accent-gold: #ff7a00;
  --accent-red: #e53e3e;
  --accent-green: #38a169;
  --accent-blue: #3182ce;
  --text-primary: #f7fafc;
  --text-secondary: #cbd5e0;
  --border-color: #4a5568;
  --shadow-dark: rgba(0, 0, 0, 0.6);
  --glow-gold: rgba(255, 122, 0, 0.3);
}

/* Estilos generales */
body {
   background: #181818;
   color: #e0e0e0;
   font-family: 'Press Start 2P', 'VT323', 'Consolas', 'monospace', serif;
   margin: 0;
   image-rendering: pixelated;
}
.main {
  max-width: 1000px;
  margin: 0 auto;
  padding: 32px 20px;
  background: linear-gradient(135deg, var(--bg-dark) 0%, var(--bg-secondary) 100%);
  border: 2px solid var(--accent-gold);
  border-radius: 15px;
  box-shadow: 
    0 0 0 4px rgba(255, 122, 0, 0.1), 
    0 10px 40px var(--shadow-dark);
}
h1 {
  text-align: center;
  font-size: clamp(1.8em, 4vw, 2.5em);
  margin-bottom: 0.8em;
  font-family: 'Cinzel', serif;
  letter-spacing: 1px;
  color: var(--accent-gold);
  text-shadow: 
    2px 2px 4px rgba(0,0,0,0.5),
    0 0 15px var(--glow-gold);
}
.section {
  background: var(--bg-panel);
  border-radius: 15px;
  border: 1px solid var(--border-color);
  margin-bottom: 25px;
  padding: 25px;
  box-shadow: 
    0 4px 15px var(--shadow-dark),
    inset 0 1px 0 rgba(255, 255, 255, 0.1);
  font-family: 'Roboto Mono', monospace;
  font-size: 1em;
}
.section h2 {
  color: var(--accent-gold);
  margin-bottom: 20px;
  font-family: 'Cinzel', serif;
  font-size: 1.4em;
  border-bottom: 2px solid var(--accent-gold);
  padding-bottom: 8px;
}
.choices {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
  gap: 15px;
  margin-bottom: 20px;
}
.choices label {
  background: linear-gradient(135deg, var(--bg-secondary), #3a4553);
  border: 2px solid var(--border-color);
  border-radius: 12px;
  padding: 15px 20px;
  cursor: pointer;
  font-family: 'Roboto Mono', monospace;
  font-size: 0.95em;
  color: var(--text-primary);
  box-shadow: 0 4px 8px rgba(0,0,0,0.3);
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  display: flex;
  align-items: center;
  font-weight: 500;
}
.choices input[type=radio], 
.choices input[type=checkbox] {
  margin-right: 12px;
  accent-color: var(--accent-gold);
  width: 16px;
  height: 16px;
}
.choices label:hover {
  background: linear-gradient(135deg, var(--accent-gold), #e06800);
  border-color: var(--accent-gold);
  color: var(--bg-dark);
  transform: translateY(-2px);
  box-shadow: 0 6px 20px var(--glow-gold);
}
.choices input:checked + label {
  background: linear-gradient(135deg, var(--accent-gold), #e06800);
  border-color: var(--accent-gold);
  color: var(--bg-dark);
  box-shadow: 0 0 15px var(--glow-gold);
}
.btn {
  background: linear-gradient(135deg, var(--bg-panel), #3a4553);
  color: var(--text-primary);
  border: 2px solid var(--border-color);
  border-radius: 10px;
  padding: 12px 24px;
  font-size: 1em;
  font-family: 'Roboto Mono', monospace;
  font-weight: 600;
  cursor: pointer;
  margin: 6px;
  box-shadow: 0 4px 8px rgba(0,0,0,0.3);
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  display: inline-block;
  text-decoration: none;
}
.btn:hover {
  background: linear-gradient(135deg, var(--accent-gold), #e06800);
  color: var(--bg-dark);
  border-color: var(--accent-gold);
  transform: translateY(-2px);
  box-shadow: 0 6px 15px var(--glow-gold);
}
.btn.big-btn {
  padding: 15px 30px;
  font-size: 1.1em;
  font-weight: 700;
}
.log, .combat-log, .combat-history {
  background: var(--bg-dark);
  border: 1px solid var(--border-color);
  border-radius: 10px;
  padding: 20px;
  min-height: 120px;
  margin-top: 20px;
  font-size: 0.95em;
  font-family: 'Roboto Mono', monospace;
  color: var(--text-primary);
  box-shadow: inset 0 2px 4px rgba(0,0,0,0.3);
  max-height: 300px;
  overflow-y: auto;
}
.combat-history {
  max-height: 200px;
  font-size: 0.9em;
}
.enemy, .player {
  font-weight: bold;
  font-family: 'Roboto Mono', monospace;
  letter-spacing: 0.5px;
}
.enemy {
  color: var(--accent-red);
}
.player {
  color: var(--accent-green);
}
.shop, .shop-items {
  background: var(--bg-dark);
  border: 1px solid var(--border-color);
  border-radius: 10px;
  padding: 20px;
  margin-top: 20px;
  font-family: 'Roboto Mono', monospace;
}
.shop-item {
  background: var(--bg-secondary);
  padding: 15px;
  border-radius: 8px;
  margin-bottom: 12px;
  border: 1px solid var(--border-color);
  display: flex;
  justify-content: space-between;
  align-items: center;
  transition: all 0.3s ease;
}
.shop-item:hover {
  background: var(--bg-panel);
  border-color: var(--accent-gold);
  box-shadow: 0 0 10px var(--glow-gold);
}
.hidden { 
  display: none; 
}

/* Enhanced FX effects */
.fx-damage {
  animation: fx-damage 0.5s ease-out;
  color: var(--accent-red) !important;
  text-shadow: 
    0 0 10px var(--accent-red),
    2px 2px 4px rgba(0,0,0,0.8);
  font-weight: bold;
}
.fx-heal {
  animation: fx-heal 0.5s ease-out;
  color: var(--accent-green) !important;
  text-shadow: 
    0 0 10px var(--accent-green),
    2px 2px 4px rgba(0,0,0,0.8);
  font-weight: bold;
}
.fx-status {
  animation: fx-status 0.6s ease-out;
  color: var(--accent-blue) !important;
  text-shadow: 
    0 0 10px var(--accent-blue),
    2px 2px 4px rgba(0,0,0,0.8);
  font-weight: bold;
}
@keyframes fx-damage {
  0% { 
    transform: scale(1.2);
    filter: brightness(2) saturate(1.5);
  }
  50% {
    transform: scale(1.1);
  }
  100% { 
    transform: scale(1);
    filter: brightness(1) saturate(1);
  }
}
@keyframes fx-heal {
  0% { 
    transform: scale(1.1) translateY(-5px);
    filter: brightness(1.8) saturate(1.4);
  }
  50% {
    transform: scale(1.05) translateY(-2px);
  }
  100% { 
    transform: scale(1) translateY(0);
    filter: brightness(1) saturate(1);
  }
}
@keyframes fx-status {
  0% { 
    transform: scale(1.05);
    filter: brightness(1.5) hue-rotate(10deg);
  }
  50% {
    transform: scale(1.02);
  }
  100% { 
    transform: scale(1);
    filter: brightness(1) hue-rotate(0deg);
  }
}

/* Enhanced scrollbar */
::-webkit-scrollbar {
  width: 12px;
  background: var(--bg-dark);
}
::-webkit-scrollbar-thumb {
  background: linear-gradient(135deg, var(--bg-panel), var(--accent-gold));
  border: 1px solid var(--border-color);
  border-radius: 6px;
}
::-webkit-scrollbar-thumb:hover {
  background: linear-gradient(135deg, var(--accent-gold), #e06800);
}
::-webkit-scrollbar-track {
  background: var(--bg-secondary);
  border-radius: 6px;
}

/* Status icons and elements */
.status-icons {
  display: inline-flex;
  gap: 5px;
  margin-left: 10px;
}
.status-icon {
  position: relative;
  font-size: 1.2em;
  cursor: help;
  transition: transform 0.2s ease;
}
.status-icon:hover {
  transform: scale(1.2);
}
.dispel-btn {
  background: var(--accent-red);
  border: none;
  color: white;
  padding: 2px 6px;
  border-radius: 4px;
  cursor: pointer;
  font-size: 0.8em;
  margin-left: 3px;
  transition: background 0.2s ease;
}
.dispel-btn:hover {
  background: #c53030;
}

/* Minimap styles */
.minimap {
  display: flex;
  flex-wrap: wrap;
  gap: 5px;
  margin: 15px 0;
  padding: 15px;
  background: var(--bg-dark);
  border-radius: 10px;
  border: 1px solid var(--border-color);
}
.minimap-room {
  width: 30px;
  height: 30px;
  display: flex;
  align-items: center;
  justify-content: center;
  border: 1px solid var(--border-color);
  border-radius: 6px;
  background: var(--bg-panel);
  font-weight: bold;
  font-size: 0.8em;
  transition: all 0.3s ease;
}
.minimap-room.current {
  background: var(--accent-gold);
  color: var(--bg-dark);
  box-shadow: 0 0 10px var(--glow-gold);
}
.minimap-room.done {
  background: var(--accent-green);
  color: var(--bg-dark);
  opacity: 0.7;
}

/* Companions display */
.companions {
  background: var(--bg-dark);
  padding: 10px 15px;
  border-radius: 8px;
  margin: 15px 0;
  border-left: 4px solid var(--accent-blue);
  font-size: 0.9em;
}

/* Turn panel */
.turn-panel {
  background: var(--bg-secondary);
  border: 2px solid var(--accent-blue);
  border-radius: 12px;
  padding: 20px;
  margin: 20px 0;
  text-align: center;
}
.turn-panel b {
  color: var(--accent-gold);
  font-size: 1.2em;
  display: block;
  margin-bottom: 15px;
}

/* Enhanced styling for complete roguelike experience */

/* Inventory and Equipment Styling */
.equipment-overlay, .inventory-overlay {
  position: fixed;
  top: 0; left: 0; right: 0; bottom: 0;
  background: rgba(0,0,0,0.9);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 2000;
  backdrop-filter: blur(5px);
}

.equipment-panel, .inventory-panel {
  background: linear-gradient(135deg, var(--bg-panel), #3a4553);
  border-radius: 20px;
  padding: 30px;
  max-width: 90vw;
  max-height: 80vh;
  overflow-y: auto;
  border: 2px solid var(--accent-gold);
  box-shadow: 0 20px 40px rgba(0,0,0,0.7);
}

.equipment-slots {
  margin-bottom: 20px;
}

.equip-slot {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 10px;
  background: rgba(255,122,0,0.1);
  margin: 5px 0;
  border-radius: 8px;
  border-left: 3px solid var(--accent-gold);
}

.item-grid, .inventory-items {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
  gap: 15px;
  margin: 20px 0;
}

.item-card, .inv-item {
  background: var(--bg-secondary);
  border-radius: 12px;
  padding: 15px;
  border: 2px solid transparent;
  cursor: pointer;
  transition: all 0.3s ease;
  position: relative;
}

.item-card.common { border-color: #9e9e9e; }
.item-card.uncommon { border-color: #4caf50; }
.item-card.rare { border-color: #2196f3; }
.item-card.legendary { border-color: #ff9800; box-shadow: 0 0 20px rgba(255,152,0,0.3); }

.item-card:hover, .inv-item:hover {
  transform: translateY(-2px);
  box-shadow: 0 8px 25px rgba(255,122,0,0.3);
}

.rarity {
  display: inline-block;
  padding: 2px 8px;
  border-radius: 12px;
  font-size: 0.8em;
  font-weight: bold;
  text-transform: uppercase;
}

.rarity.common { background: #757575; color: white; }
.rarity.uncommon { background: #388e3c; color: white; }
.rarity.rare { background: #1976d2; color: white; }
.rarity.legendary { background: #f57c00; color: white; }

/* Combat Interface */
.combat-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 20px;
  margin: 20px 0;
}

.hero-panel, .enemy-panel {
  background: var(--bg-secondary);
  border-radius: 15px;
  padding: 20px;
  border: 2px solid var(--border-color);
}

.hero-panel {
  border-color: var(--accent-green);
}

.enemy-panel {
  border-color: var(--accent-red);
}

.hp-bar {
  position: relative;
  background: var(--bg-dark);
  border-radius: 20px;
  height: 25px;
  margin: 10px 0;
  overflow: hidden;
  border: 1px solid var(--border-color);
}

.hp-fill {
  height: 100%;
  background: linear-gradient(90deg, var(--accent-green), #66bb6a);
  transition: width 0.5s ease;
  border-radius: 20px;
}

.enemy .hp-fill {
  background: linear-gradient(90deg, var(--accent-red), #ef5350);
}

.hp-text {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  font-weight: bold;
  font-size: 0.9em;
  text-shadow: 1px 1px 2px rgba(0,0,0,0.8);
}

.action-panel {
  background: var(--bg-panel);
  border-radius: 15px;
  padding: 20px;
  margin-top: 15px;
}

.basic-actions, .special-actions {
  display: flex;
  flex-wrap: wrap;
  gap: 10px;
  justify-content: center;
  margin: 10px 0;
}

.combat-btn.primary {
  background: linear-gradient(135deg, var(--accent-red), #d32f2f);
  font-size: 1.1em;
  font-weight: bold;
}

.special-btn {
  background: linear-gradient(135deg, var(--accent-gold), #f57c00);
  color: var(--bg-dark);
  border: none;
  border-radius: 12px;
  padding: 12px 18px;
  font-weight: bold;
  cursor: pointer;
  transition: all 0.3s ease;
  box-shadow: 0 4px 15px rgba(255,122,0,0.3);
}

.special-btn:hover {
  transform: translateY(-2px);
  box-shadow: 0 6px 20px rgba(255,122,0,0.4);
}

.special-btn:disabled {
  opacity: 0.5;
  cursor: not-allowed;
  transform: none;
}

/* Conditions */
.conditions {
  display: flex;
  flex-wrap: wrap;
  gap: 5px;
  margin-top: 10px;
}

.condition {
  background: var(--accent-blue);
  color: white;
  padding: 2px 8px;
  border-radius: 12px;
  font-size: 0.8em;
  font-weight: bold;
}

.condition.rage { background: var(--accent-red); }
.condition.poison { background: #7b1fa2; }
.condition.blessed { background: var(--accent-gold); }

/* Merchant Interface */
.merchant-section {
  max-width: 1000px;
  margin: 0 auto;
}

.gold-display {
  text-align: center;
  font-size: 1.2em;
  margin: 15px 0;
  padding: 10px;
  background: linear-gradient(135deg, var(--accent-gold), #f57c00);
  color: var(--bg-dark);
  border-radius: 25px;
  font-weight: bold;
}

.merchant-stock {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
  gap: 15px;
  margin: 20px 0;
}

.merchant-item {
  background: var(--bg-secondary);
  border-radius: 15px;
  padding: 20px;
  border: 2px solid var(--border-color);
  display: flex;
  justify-content: space-between;
  align-items: center;
  transition: all 0.3s ease;
}

.merchant-item:hover {
  transform: translateY(-3px);
  box-shadow: 0 10px 25px rgba(0,0,0,0.3);
}

.item-stats {
  display: flex;
  gap: 10px;
  align-items: center;
  margin-top: 8px;
}

.price {
  background: var(--accent-gold);
  color: var(--bg-dark);
  padding: 4px 10px;
  border-radius: 15px;
  font-weight: bold;
  font-size: 0.9em;
}

.buy-btn {
  background: linear-gradient(135deg, var(--accent-green), #388e3c);
  color: white;
  border: none;
  border-radius: 12px;
  padding: 12px 20px;
  font-weight: bold;
  cursor: pointer;
  transition: all 0.3s ease;
  min-width: 80px;
}

.buy-btn:hover:not(:disabled) {
  background: linear-gradient(135deg, #66bb6a, #43a047);
  transform: scale(1.05);
}

.buy-btn:disabled {
  background: #616161;
  cursor: not-allowed;
  opacity: 0.6;
}

.merchant-actions {
  display: flex;
  justify-content: center;
  gap: 15px;
  margin-top: 25px;
}

.merchant-btn {
  background: var(--bg-panel);
  color: var(--text-primary);
  border: 2px solid var(--accent-gold);
  border-radius: 12px;
  padding: 12px 25px;
  font-weight: bold;
  cursor: pointer;
  transition: all 0.3s ease;
}

.merchant-btn:hover {
  background: var(--accent-gold);
  color: var(--bg-dark);
}

/* Character Selection */
.char-selection .character-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
  gap: 20px;
  margin: 25px 0;
}

.char-card {
  background: linear-gradient(135deg, var(--bg-panel), #3a4553);
  border-radius: 20px;
  padding: 25px;
  border: 2px solid var(--border-color);
  cursor: pointer;
  transition: all 0.3s ease;
  text-align: center;
}

.char-card:hover {
  border-color: var(--accent-gold);
  transform: translateY(-5px);
  box-shadow: 0 15px 35px rgba(255,122,0,0.3);
}

.char-special {
  color: var(--accent-gold);
  font-style: italic;
  margin: 10px 0;
  font-size: 0.9em;
}

.char-stats {
  background: rgba(255,122,0,0.1);
  padding: 8px;
  border-radius: 8px;
  margin-top: 15px;
}

/* Welcome Screen */
.features-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
  gap: 15px;
  margin: 25px 0;
}

.feature-card {
  background: linear-gradient(135deg, var(--accent-gold), #f57c00);
  color: var(--bg-dark);
  padding: 15px;
  border-radius: 15px;
  text-align: center;
  font-weight: bold;
  font-size: 0.9em;
  box-shadow: 0 5px 15px rgba(255,122,0,0.3);
  transition: transform 0.3s ease;
}

.feature-card:hover {
  transform: translateY(-3px);
}

.start-btn.epic {
  background: linear-gradient(135deg, var(--accent-gold), #f57c00);
  color: var(--bg-dark);
  font-size: 1.3em;
  padding: 18px 35px;
  box-shadow: 0 8px 25px rgba(255,122,0,0.4);
  animation: epicPulse 2s infinite ease-in-out;
}

@keyframes epicPulse {
  0%, 100% { transform: scale(1); }
  50% { transform: scale(1.05); }
}

/* Responsive Design */
@media (max-width: 768px) {
  .combat-grid {
    grid-template-columns: 1fr;
    gap: 15px;
  }
  
  .merchant-stock {
    grid-template-columns: 1fr;
  }
  
  .char-selection .character-grid {
    grid-template-columns: 1fr;
  }
  
  .features-grid {
    grid-template-columns: repeat(2, 1fr);
  }
  
  .basic-actions, .special-actions {
    flex-direction: column;
  }
  
  .equipment-panel, .inventory-panel {
    padding: 20px;
    margin: 10px;
  }
}
